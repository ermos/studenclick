import Vue from 'vue'
import Vuex from 'vuex'
import tooltip from "@/store/modules/tooltip";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tooltip: tooltip,
  }
})
